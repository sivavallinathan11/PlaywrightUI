# setup
```
#install deps
npm install
#playwright setup - install default browser.
npx playwright install
#run tests in chrome
npx playwright test
or
npx playwright test --headed
npx playwright test --repeat-each 10 tests/(testpath) -> This will run in number of iteration
```